## Quick Sort:

### Introduction:
* Pivot element (swap in a way: everything before pivot is less than the pivot element and everything after pivot is greater than the pivot element)

### Pivot choosing methods:
* First element as the pivot
* Last element as the pivot
* Middle
* Random
* Median of three (first, mid, last)
* Median of all

### Partition methods
